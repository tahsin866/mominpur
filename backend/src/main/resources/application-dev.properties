# ============================================
# DEV PROFILE — Local PostgreSQL
# Chalate: ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
# ============================================

server.port=8080

# Local database (nijer username/password boshan)
spring.datasource.url=jdbc:postgresql://localhost:5432/mominpur
spring.datasource.username=postgres
spring.datasource.password=root

# Local e schema auto-update thik ache
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File upload local folder e jabe
upload.dir=./run/media/xaki-taki/Al JAMI/muminpur_file