server.port=8080

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://76.13.183.6:5432/mominpur
spring.datasource.username=mominpur_user
spring.datasource.password=7xr&d6Wn@XQm4aK&

# Hibernate
# Local/dev: update (auto-schema). Prod: validate (never auto-change prod schema).
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# File Upload
upload.dir=${UPLOAD_DIR:/root/storage}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
